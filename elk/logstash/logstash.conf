input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.28.jar"
    jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://my-database:3306/mydocker02"
    jdbc_user => "root"
    jdbc_password => "root"
    schedule => "* * * * *"
    use_column_value => true
    tracking_column => "createdat"
    tracking_column_type => "numeric"
    last_run_metadata_path => "./aaa.txt"
    statement => "select id, title, contents, centerName, centerOwnerName, centerPhone, recruitCount, serviceTime, unix_timestamp(serviceDate) as serviceDate, address, addressDetail, location1, location2, unix_timestamp(createdAt) as createdAt, createdAt as createdAt2, userId from board where unix_timestamp(createdAt) > :sql_last_value and deletedAt is null order by createdAt"
  }
}

output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    index => "board"
    template => "/usr/share/logstash/post_template.json"
  }
}